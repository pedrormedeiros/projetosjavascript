var numero 

numero = prompt("Digite o número que deseja fazer a mutiplição")

for (contador = 1; contador <=100; contador = contador +1){
    var resultado = numero * contador;
    alert(numero + " x " + contador + " = " + resultado);
}